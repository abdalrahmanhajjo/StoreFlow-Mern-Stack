# Copy to .env and fill in real values. Never commit .env.

# Server (avoid 5000 on macOS — AirPlay Receiver squats on it)
PORT=5050
NODE_ENV=development

# Database — either a local MongoDB or an Atlas cluster.
# Atlas: URL-encode special characters in the password (@ -> %40).
# MONGO_URI=mongodb+srv://<user>:<encoded-password>@<cluster-host>/storeflow?retryWrites=true&w=majority
MONGO_URI=mongodb://localhost:27017/storeflow

# Auth
JWT_ACCESS_SECRET=change-me

# Frontend origin (CORS + links in emails)
CLIENT_APP_URL=http://localhost:5175

# Outgoing email — Brevo transactional email over HTTPS (works on hosts that
# block SMTP, like Render's free plan). Create a free Brevo account, verify a
# sender address (your Gmail is fine), and make an API key.
# Leave BREVO_API_KEY empty for local dev — codes print to the console instead.
BREVO_API_KEY=
BREVO_SENDER_EMAIL=          # a verified sender, e.g. you@gmail.com
BREVO_SENDER_NAME=StoreFlow
