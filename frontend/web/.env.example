# Copy to .env (gitignored). When set, the app uses the real API; when unset,
# it defaults to "/api" (same-origin) — works when the backend serves the
# frontend build. For local dev set: VITE_API_BASE_URL=http://localhost:5050/api
VITE_API_BASE_URL=http://localhost:5050/api
