# Copy to .env (gitignored). With VITE_API_BASE_URL set, auth runs against the
# real backend; leave it unset to run the app fully on the built-in demo mocks.
VITE_API_BASE_URL=http://localhost:5050/api
